from django.apps import AppConfig


class CsvUploadConfig(AppConfig):
    """Configuration for the CSV upload app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "csv_upload"
